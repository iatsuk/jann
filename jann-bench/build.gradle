dependencies {
    implementation(
            project(':jann-api'),
            deps.jmh,
            deps.annoyj,
            deps.annoy4s,
            deps.guava,
    )

    annotationProcessor (
            deps.jmh,
    )
}
